[package]
name = "m87-shared"
version = "0.0.41-dev0"
edition = "2024"
license = "Apache-2.0"
description = "Shared types and utilities for the make87 platform"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
hex = "0.4"
